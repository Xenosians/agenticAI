ROUTING CONTRACT REPAIR ATTEMPT

The previous routing attempt was rejected by trusted application
validation.

Recompute the routing plan FROM SCRATCH from the ORIGINAL USER REQUEST.

Do not preserve or imitate the rejected plan merely because it was
generated previously.

All ordinary routing, grounding, capability, and semantic-intent rules
remain mandatory.

A direct user-requested governed mutation must remain a direct mutation
when one available mutation capability already represents that exact
operation.

Do NOT synthesize an extra read delegation merely to check execution
preconditions that the governed mutation capability itself owns.

Examples of trusted mutation preconditions include:

- whether a requested local Git branch exists;
- whether the Git working tree is clean;
- whether a target remains safe to mutate;
- whether an approved operation remains valid immediately before
  execution.

Those checks belong to trusted application policy unless the ORIGINAL
USER REQUEST explicitly asks for a result-dependent workflow.

Example:

    ORIGINAL USER REQUEST:
        Switch the AI repository to test/example.

If the available governed branch-switch capability directly represents
that operation, emit exactly ONE mutation delegation for branch
switching.

Do NOT produce:

    list branches
        then
    conditionally switch

unless the ORIGINAL USER REQUEST itself explicitly requested such a
read-dependent sequence.

A conditional workflow is appropriate only when the USER explicitly
makes one requested operation depend on a trusted read result.

Example:

    If bob's account is locked, unlock it.

Never invent condition fields.

Never weaken, remove, reinterpret, or broaden grounded values in order
to repair a rejected plan.

Return only the corrected JSON object.
No markdown.
No explanation.
