ROUTING CONTRACT REPAIR ATTEMPT

The previous routing attempt was rejected by trusted application
validation.

Recompute the routing plan FROM SCRATCH from the ORIGINAL USER REQUEST.

Do not preserve an invalid plan merely because it was generated
previously.

All ordinary capability, semantic-intent, grounding, scope, and
exclusion rules remain mandatory.

============================================================
REPAIR RULES
============================================================

1. Preserve the operation actually requested by the user.

2. If one available capability directly represents an unconditional
   requested operation, prefer one direct delegation.

3. If that capability declares:

       intent_metadata.policy_owns_preconditions = true

   do NOT synthesize another read merely to duplicate those
   preconditions.

4. Trusted execution policy owns those preconditions.

5. Preserve genuine result-dependent user intent.

6. If the ORIGINAL USER REQUEST explicitly makes one operation depend
   on another result, keep the workflow conditional.

7. For a conditional repair:
   - use an earlier unconditional read
   - use only a result_field exposed through trusted condition_fields
   - never invent a result field
   - keep the mutation conditional

8. Never repair an invalid conditional workflow by silently converting
   an explicitly conditional user request into an unconditional
   mutation.

9. Never repair an invalid direct request by adding unrequested
   operations.

10. Never weaken grounding or exclusions to make a plan validate.

11. Never broaden or narrow the target.

12. Never invent a replacement capability, argument, identifier, or
    condition field.

13. If no safe valid plan can represent the original request, require
    clarification or return no executable delegation rather than
    fabricating one.

Return only the corrected JSON object.
No markdown.
No explanation.
